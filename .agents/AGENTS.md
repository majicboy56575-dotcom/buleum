# Project Rules

- **Git Pushing Constraint**: Do NOT run `git push` or push changes to GitHub until the user explicitly requests it. You may perform local commits if appropriate, but never push automatically.
